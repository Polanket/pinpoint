<%= simple_form_for([ map, user_group ]) do |f|  %>
  <div class="form-inputs">
  <%= f.association :user, label_method: :name, input_html: { multiple: true, class: 'select2' },label: 'Add friends to the map' %>
  </div>
  <%= f.button :submit, "ADD FRIEND", class: "addfriend-btn" %>
<% end %>

