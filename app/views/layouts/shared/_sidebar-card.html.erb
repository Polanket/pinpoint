<div class= "sidebar-card" >
  <div>
    <% if map.photo.present? %>
      <%= cl_image_tag map.photo.url(:avatar), class: "avatar"  %>
    <% else %>
      <%= image_tag "globe.png",  class: "avatar" %>
    <% end %>
  </div>
  <div class="sidebar-card-infos">
    <div class= "content">
      <h2><%= map.name %> </h2>
      <p><%= map.description %></p>
    </div>
  </div>
</div>

