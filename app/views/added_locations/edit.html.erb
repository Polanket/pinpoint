<div class="location-page">

  <div class="location-image-container">
    <%= image_tag @location.photo.first, class:"location-image" %>
  </div>

  <div class="location-content-container">
    <div class="location-details">
      <h1><%= @location.name %></h1>
      <p><%= @location.types.first.capitalize %></p>
    </div>
    <p><%= @location.address %></p>
  </div>

  <div class="location-tags-container">

  <%= simple_form_for [@map, @location] do |f| %>
    <div class="field">
      <%= f.input :tag_list, collection: AddedLocation.categories, selected: 'fun', input_html: { multiple: true, class: 'select2', style: 'font-family:"Karla", "Helvetica", "sans-serif";' }, label: "Add tags!" %>
      <%= f.submit "Add Tags", class: "main-btn", style: "margin-top: 40px;" %>
    </div>
  <% end %>

  <div class="location-description-container">

  </div>

  <div class="location-photo-container">

  </div>

  <div class="location-reviews-container">

  </div>


</div>
