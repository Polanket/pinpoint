<%= simple_form_for(@review ,url: map_added_location_reviews_path(@map, @added_location), remote: true) do |form| %>
  <%= form.input :content, as: :text %>
  <%= form.submit "ADD REVIEW", class: "tag-review-btn" %>
<% end %>
